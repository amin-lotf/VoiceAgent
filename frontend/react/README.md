# React UI Wrapper

The React source now lives at `src/voice_agent/frontend/react`.

This wrapper exists so older local commands still work:

```bash
cd frontend/react
npm run dev
```

Equivalent direct path:

```bash
cd src/voice_agent/frontend/react
npm run dev
```
